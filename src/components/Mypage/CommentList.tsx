import CommentItem from './CommentItem';
import { ListWrapper } from './LikeMediaList';

const CommentList = () => {
  // 원준 일하는 중~
  // const getMyComments =
  return (
    <ListWrapper>
      <CommentItem />
      <CommentItem />
      <CommentItem />
    </ListWrapper>
  );
};

export default CommentList;
